def mainClassName = "pro.gravit.launchermodules.unsafecommands.UnsafeCommandsModule"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

jar {
    manifest.attributes("Module-Main-Class": mainClassName)
}
