def mainClassName = "pro.gravit.launchermodules.mojangsupport.ModuleImpl"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

jar {
    manifest.attributes("Module-Main-Class": mainClassName)
}
