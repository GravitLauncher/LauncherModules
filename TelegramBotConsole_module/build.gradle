def mainClassName = "pro.gravit.launchermodules.telegrambot.TelegramBotModule"

sourceCompatibility = '21'
targetCompatibility = '21'

dependencies {
    implementation group: 'org.telegram', name: 'telegrambots', version: '6.0.1'
}

jar {
    manifest.attributes("Module-Main-Class": mainClassName)
}
