def mainClassName = "ru.gravit.launcher.addhash.HashModule"

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

dependencies {
	compile 'org.mindrot:jbcrypt:0.4'
}
jar {
	manifest.attributes("Main-Class": mainClassName)
}